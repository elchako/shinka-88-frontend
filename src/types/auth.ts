// интерфейс стейта
export interface IinitialState {
    name: string,
    phone: string
    regModal: boolean
    smsCode: boolean
    oftensmsCodeReq: boolean
    token: string
    checkProcess: boolean
}